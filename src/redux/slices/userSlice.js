import { createSlice } from '@reduxjs/toolkit';

const userSlice = createSlice({
  name: 'profileUser',
  initialState: [],
  reducers: {},
  extraReducers: () => {},
});

export default userSlice.reducers;
